<p id="notice"><%= notice %></p>

<%= form_for(@drop, url: choose_new_or_edit) do |f| %>
  <%= f.error_messages! %>

  <%= f.label "味" %>
  <%= f.text_field :content, class: "form-control" %>
  <br>
  <%= f.submit "いれる", class:"btn btn-primary btn-block"%>
<% end %>
